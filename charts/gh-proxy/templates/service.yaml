apiVersion: v1
kind: Service
metadata:
  name: {{ include "gh-proxy.fullname" . }}
  labels:
    {{- include "gh-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gh-proxy.selectorLabels" . | nindent 4 }}

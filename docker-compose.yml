services:
  proxy:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      GITHUB_USER: ${GITHUB_USER}
      GITHUB_TOKEN: ${GITHUB_TOKEN}
      PORT: ${PORT:-8080}
    restart: unless-stopped
